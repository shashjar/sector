/**
 * San Carlos (KSQL) is where the scope opens.
 */
export const DEFAULT_AIRPORT = "KSQL";

export const DEFAULT_CENTER = { lat: 37.51313, lon: -122.250838 };

/**
 * Roughly 33 nm across on a desktop viewport.
 */
export const DEFAULT_ZOOM = 11;
